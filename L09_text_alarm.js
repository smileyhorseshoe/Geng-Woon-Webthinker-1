let xpos = width /2
let ypos = height/2

function setup() {
    createCanvas(600,400);
    background(0);
    fill("cyan")
    textSize(28)
    textAlign(CENTER,CENTER)
    
}

function draw() {
    
    text("bounce!", width/2, height/2)
}
